services:
  sync:
    build: .
    volumes:
      - ./config.yaml:/config.yaml:ro
    command: --config /config.yaml
  # db:
  #   image: postgres
  #   restart: always
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   env_file:
  #     - .env

volumes:
  db_data:
